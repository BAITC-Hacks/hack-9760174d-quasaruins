# README media

Captured from the running app at commit `d5db129` (including Claude’s final downtown density, height and landmark polish) on 23 September 2026.

- `city-overview.jpg`: the city focused on Nura, with the project hand visible.
- `construction-replay.gif`: 65 captured frames of Example → Start → the quarter-eight result, encoded at 4 frames per second and 960 × 540 pixels.

These are app captures, not design mockups. The capture used a separate local preview with live AI disabled. The project artwork inside the app is AI-generated; see [art provenance](../PROJECT-ART.md).
